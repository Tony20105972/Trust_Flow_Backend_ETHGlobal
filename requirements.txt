# requirements.txt
# This file lists all Python package dependencies required for the project.
# To install these packages, run: pip install -r requirements.txt

# Web Framework
fastapi~=0.111.0
uvicorn[standard]~=0.30.1 # ASGI server for FastAPI

# Web3 & Blockchain Interaction
web3~=6.15.0             # Python library for interacting with Ethereum
py-solc-x~=1.1.1         # Python wrapper for the Solidity compiler (solc)

# Environment Variables & Configuration
python-dotenv~=1.0.0     # For loading environment variables from .env files
PyYAML~=6.0.1            # For parsing YAML configuration files (config.yaml)

# AI / LLM Integration
groq~=0.9.0             # For Groq API integration
openai~=1.35.7          # For OpenAI API integration

# IPFS Interaction (using requests for HTTP calls, e.g., to Pinata/Infura)
requests~=2.32.3        # HTTP library for making API calls (e.g., to IPFS pinning services)

# Testing (for local development, usually not needed in production deploys)
pytest~=8.2.2           # Testing framework for running unit tests

# Add any other specific libraries your project uses below:
# 예를 들어, 데이터베이스 클라이언트, 다른 유틸리티 등
# sqlalchemy
# psycopg2-binary
# google-cloud-storage

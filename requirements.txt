# ✅ 최소 구성 requirements.txt (Render 배포용)

fastapi
uvicorn

web3==5.31.3      # ExtraDataToPOAMiddleware 위해 5.x 고정
py-solc-x         # Solidity compiler

groq              # Groq API client

requests          # API 호출용 (IPFS 등)

# 선택: 테스트 필요 없으면 pytest 삭제 가능

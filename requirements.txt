# requirements.txt
# Install all packages with: pip install -r requirements.txt

# Web Framework
fastapi~=0.111.0
uvicorn[standard]~=0.30.1   # ASGI server for FastAPI

# Web3 & Blockchain Interaction
web3~=6.15.0                # Ethereum interaction
py-solc-x~=1.1.1            # Solidity compiler wrapper

# Environment & Config
python-dotenv~=1.0.0        # Load env vars from .env
PyYAML~=6.0.1               # Parse config.yaml

# AI / LLM Integration
groq~=0.9.0                 # ✅ Groq API client (new)

# HTTP & IPFS
requests~=2.32.3            # For IPFS / API calls

# Testing
pytest~=8.2.2               # Unit testing
